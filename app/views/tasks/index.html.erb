<h1>AI ToDo プランナー</h1>
<%= link_to '+ タスクを作成', new_task_path, class: 'btn btn-primary' %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <span class="completed-mark"><%= link_to (task.completed ? '✅' : '🔲'), complete_task_path(task), method: :put %></span>
      <%= link_to task.title, task_path(task) %>
      （未完了のサブタスク: <%= task.sub_tasks.where(completed: false).count %>）
    </li>
  <% end %>
</ul>
