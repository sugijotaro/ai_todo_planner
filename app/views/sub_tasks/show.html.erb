<h1><%= @sub_task.content %></h1>

<%= link_to (@sub_task.completed ? '未完了にする' : '完了にする'), complete_task_sub_task_path(@task, @sub_task), method: :put %>
<br>
<%= link_to '削除', [@task, @sub_task], method: :delete, data: { confirm: '本当に削除しますか？' } %>
<%= link_to '戻る', task_path(@task) %>
